---

system_packages:
  - python3-dev
  - git
  - g++
  - gcc
  - libpq-dev
  - postgresql
  - postgresql-common
  - postgresql-contrib
  - python3-psycopg2
  - nginx
  - vim
  # needed for the ansible apt_repository module
  - python-apt-dev
#  - libsemanage-python
  - python3
  - liblz4-tool
  - zstd
  - virtualenv

ssl_requirements:
  - openssl
  - libssl-dev

circus_system_packages:
  - libzmq-dev
  - libevent-dev
